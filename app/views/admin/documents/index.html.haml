%h2 Dokumenty
%ul.action_links
  %li.create_new= link_to "Přidat nový dokument", new_admin_document_path

- unless documents.empty?
  %h3.panel Seznam dokumentů

  %table#documents.table_overview
    %thead
      %tr
        %th Název dokumentu
        %th Přiřazen v obchodech
        %td Akce
    %tbody
      - for document in documents
        %tr{class: cycle("odd", "even")}
          %th= document.name
          %td= document.assigned_to_shops
          %td
            %ul.actions
              %li.edit= link_to "Upravit", edit_admin_document_path(document)
              %li.delete= link_to "Smazat", admin_document_path(document), method: :delete, confirm: "Opravdu smazat dokument #{document.name}?"

  = paginate documents
- else
  .empty_set
    Zatím nejsou vytvořeny žádné dokumenty