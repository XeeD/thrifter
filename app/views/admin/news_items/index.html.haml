%h2 Novinky v obchodu #{shop.name}
%ul.action_links
  %li.create_new= link_to "Přidat novinku", new_admin_shop_news_item_path(shop)

- unless news_items_paginated.empty?
  %h3.panel Seznam novinek

  %table#news_items.table_overview
    %thead
      %tr
        %th Nadpis
        %td Akce
    %tbody
      - for news_item in news_items_paginated
        %tr
          %th= news_item.title
          %td
            %ul.actions
              %li.edit= link_to "Upravit", edit_admin_shop_news_item_path(shop, news_item)
              %li.delete= link_to "Smazat", admin_shop_news_item_path(shop, news_item), method: :delete, confirm: "Opravdu smazat novinku #{news_item.title}?"

  = paginate news_items_paginated

- else
  .empty_set
    Zatím nejsou vytvořeny žádné novinky