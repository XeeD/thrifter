%h2 Šablony parametrů
%ul.action_links
  %li.create_new= link_to "Přidat novou šablonu parametrů", new_admin_param_template_path

- unless param_templates.empty?
  %h3.panel Seznam šablon parametrů

  %table#param_templates.table_overview
    %thead
      %tr
        %th Název šablony
        %td Akce
    %tbody
      - for template in param_templates
        %tr{class: cycle("odd", "even")}
          %th= template.name
          %td
            %ul.actions
              %li.show= link_to "Zobrazit", admin_param_template_path(template)
              %li.edit= link_to "Upravit", edit_admin_param_template_path(template)
              %li.delete= link_to "Smazat", admin_param_template_path(template), method: :delete, confirm: "Opravdu smazat šablonu #{template.name}?"

  = paginate param_templates

- else
  .empty_set
    Zatím nejsou vytvořeny žádné šablony parametrů
