%h2 Značky
%ul.action_links
  %li.create_new= link_to "Přidat novou značku", new_admin_brand_path

- unless brands.empty?
  %h3.panel Seznam značek

  %table#brands.table_overview
    %thead
      %tr
        %th Název značky
        %td Akce
    %tbody
      - for brand in brands
        %tr{class: cycle("odd", "even")}
          %th= brand.name
          %td
            %ul.actions
              %li.edit= link_to "Upravit", edit_admin_brand_path(brand)
              %li.delete= link_to "Smazat", admin_brand_path(brand), method: :delete, confirm: "Opravdu smazat značku #{brand.name}?"

- else
  .empty_set
    Zatím nejsou vytvořeny žádné značky